"""Best-effort legacy migration from `ha-family-hub` - not a v1 gate (see the rebuild
plan's "Decided" section: this product's target buyer is a new install, not someone
migrating off a mature KidsChores/ChoreOps setup - not worth over-investing in).

See `legacy_import.py`.
"""
