"""Dashboard generation/registration system - STUB, not yet implemented.

See `registry.py` (template + registry system) and `register.py` (Lovelace registration).
Deliberately not wired into `__init__.py`'s `async_setup_entry` yet - see the TODO there for
why (building a dashboard around stub/empty modules would just produce an empty shell).
"""
